package tankrotationexample;

import java.awt.image.BufferedImage;

public class SpeedPowerUp extends PowerUp {
    public SpeedPowerUp(int x, int y, boolean vis, BufferedImage image) {
        super(x, y, vis, image);
    }
}