package tankrotationexample;

import java.awt.image.BufferedImage;

public class HardWall extends Wall {
    public HardWall(int x, int y, boolean vis, BufferedImage image) {
        super(x, y, vis, image);
    }

    @Override
    public void update() {

    }
}
